[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rc_simulacion"
version = "1.0"
description = "Proyecto Final de Redes"

[tool.setuptools.packages.find]
where = ["."]
include = ["rc_simulacion*"]